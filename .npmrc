# pnpm + Expo compatibility: Expo's config-plugin resolver
# (@expo/config-plugins) uses require.resolve from the project root and
# can't follow pnpm's nested symlink layout. Hoisting flattens
# node_modules like npm/yarn does, which the plugin resolver expects.
node-linker=hoisted
public-hoist-pattern[]=*
